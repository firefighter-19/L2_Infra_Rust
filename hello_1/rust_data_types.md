# Типы данных в Rust

Этот документ содержит примеры всех основных типов данных в Rust и способы их использования.

## Скалярные типы (Scalar Types)

### Целые числа (Integer Types)

#### Знаковые целые числа (Signed Integers)
```rust
let x: i8 = -128;      // 8-битное число: от -128 до 127
let y: i16 = -32768;   // 16-битное число: от -32768 до 32767
let z: i32 = -2147483648; // 32-битное число: от -2^31 до 2^31-1
let w: i64 = -9223372036854775808; // 64-битное число
let v: i128 = -170141183460469231731687303715884105728; // 128-битное число
let size: isize = -100; // Размер зависит от архитектуры (32 или 64 бита)
```

#### Беззнаковые целые числа (Unsigned Integers)
```rust
let a: u8 = 255;       // 8-битное число: от 0 до 255
let b: u16 = 65535;    // 16-битное число: от 0 до 65535
let c: u32 = 4294967295; // 32-битное число: от 0 до 2^32-1
let d: u64 = 18446744073709551615; // 64-битное число
let e: u128 = 340282366920938463463374607431768211455; // 128-битное число
let usize_var: usize = 100; // Размер зависит от архитектуры
```

#### Литералы целых чисел
```rust
let decimal = 98_222;        // Десятичная система: 98222
let hex = 0xff;              // Шестнадцатеричная: 255
let octal = 0o77;            // Восьмеричная: 63
let binary = 0b1111_0000;     // Двоичная: 240
let byte = b'A';              // Байт (только u8): 65
```

### Числа с плавающей точкой (Floating-Point Types)
```rust
let f: f32 = 3.14;     // 32-битное число с плавающей точкой (одинарная точность)
let g: f64 = 2.71828;  // 64-битное число с плавающей точкой (двойная точность)

// По умолчанию используется f64
let default_float = 3.14; // Тип: f64
```

### Булевы значения (Boolean Type)
```rust
let t = true;          // Тип: bool
let f: bool = false;   // Явное указание типа

// Использование в условиях
if t {
    println!("Это правда!");
}
```

### Символы (Character Type)
```rust
let c = 'z';           // Символ Unicode (4 байта)
let z = 'ℤ';           // Поддерживает Unicode
let heart_eyed_cat = '😻'; // Эмодзи

// Символы отличаются от строк
let char_var: char = 'A';
let string_var: &str = "A"; // Это строка, а не символ
```

## Составные типы (Compound Types)

### Кортежи (Tuples)
```rust
// Создание кортежа
let tup: (i32, f64, u8) = (500, 6.4, 1);

// Деструктуризация кортежа
let (x, y, z) = tup;
println!("Значение y: {}", y); // 6.4

// Доступ по индексу
let five_hundred = tup.0;  // 500
let six_point_four = tup.1; // 6.4
let one = tup.2;            // 1

// Кортеж без значений (unit type)
let unit = (); // Тип: ()
```

### Массивы (Arrays)
```rust
// Массив фиксированного размера
let a = [1, 2, 3, 4, 5];
let months = ["Январь", "Февраль", "Март", "Апрель", "Май", "Июнь",
              "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь"];

// Явное указание типа и размера
let b: [i32; 5] = [1, 2, 3, 4, 5];

// Инициализация массива одинаковыми значениями
let c = [3; 5]; // [3, 3, 3, 3, 3]

// Доступ к элементам
let first = a[0];  // 1
let second = a[1]; // 2

// Размер массива известен на этапе компиляции
println!("Длина массива: {}", a.len()); // 5
```

### Срезы (Slices)
```rust
let a = [1, 2, 3, 4, 5];

// Создание среза
let slice = &a[1..3];  // [2, 3] - элементы с индекса 1 до 3 (не включая 3)
let slice2 = &a[..3];  // [1, 2, 3] - от начала до индекса 3
let slice3 = &a[2..];  // [3, 4, 5] - от индекса 2 до конца
let slice4 = &a[..];   // Весь массив

// Срезы строк
let s = String::from("hello world");
let hello = &s[0..5];   // "hello"
let world = &s[6..11];  // "world"
```

## Строки (Strings)

### Строковый срез (&str)
```rust
// Строковый литерал (неизменяемый)
let s: &str = "Привет, мир!";
let s2 = "Hello, world!"; // Тип: &str

// Строковые срезы
let hello = &s[0..6]; // "Привет"
```

### Строка (String)
```rust
// Создание String
let mut s = String::from("hello");
s.push_str(", world!"); // Добавление строки
s.push('!');            // Добавление символа

// Конкатенация
let s1 = String::from("Hello, ");
let s2 = String::from("world!");
let s3 = s1 + &s2; // s1 перемещается, s2 остается

// Использование format! макроса
let s1 = String::from("tic");
let s2 = String::from("tac");
let s3 = String::from("toe");
let s = format!("{}-{}-{}", s1, s2, s3); // "tic-tac-toe"
```

## Ссылки и указатели

### Неизменяемые ссылки (&)
```rust
let x = 5;
let y = &x;        // y - ссылка на x
println!("{}", *y); // Разыменование: 5
```

### Изменяемые ссылки (&mut)
```rust
let mut x = 5;
let y = &mut x;    // Изменяемая ссылка
*y += 1;           // Изменение значения через ссылку
println!("{}", x); // 6
```

### Box<T> (Умный указатель в куче)
```rust
let b = Box::new(5); // Значение 5 хранится в куче
println!("b = {}", b); // Автоматическое разыменование

// Рекурсивные типы
enum List {
    Cons(i32, Box<List>),
    Nil,
}
```

## Коллекции (Collections)

### Vec<T> (Вектор)
```rust
// Создание вектора
let v: Vec<i32> = Vec::new();
let v = vec![1, 2, 3]; // Макрос для создания вектора

// Добавление элементов
let mut v = Vec::new();
v.push(1);
v.push(2);
v.push(3);

// Доступ к элементам
let third: &i32 = &v[2];        // Паникует, если индекс вне границ
let third: Option<&i32> = v.get(2); // Возвращает Option

// Итерация
for i in &v {
    println!("{}", i);
}

// Итерация с изменением
for i in &mut v {
    *i += 50;
}
```

### HashMap<K, V>
```rust
use std::collections::HashMap;

// Создание HashMap
let mut scores = HashMap::new();
scores.insert(String::from("Blue"), 10);
scores.insert(String::from("Yellow"), 50);

// Доступ к значениям
let team_name = String::from("Blue");
let score = scores.get(&team_name); // Option<&V>

// Итерация
for (key, value) in &scores {
    println!("{}: {}", key, value);
}

// Обновление значений
scores.insert(String::from("Blue"), 25); // Перезапись
scores.entry(String::from("Yellow")).or_insert(50); // Вставка, если нет
```

### HashSet<T>
```rust
use std::collections::HashSet;

let mut set = HashSet::new();
set.insert(1);
set.insert(2);
set.insert(3);

// Проверка наличия элемента
if set.contains(&2) {
    println!("2 находится в множестве");
}
```

## Option<T> и Result<T, E>

### Option<T>
```rust
// Option может быть Some(T) или None
let some_number = Some(5);
let some_string = Some("строка");
let absent_number: Option<i32> = None;

// Обработка Option
match some_number {
    Some(value) => println!("Значение: {}", value),
    None => println!("Нет значения"),
}

// Использование методов Option
let x: Option<i32> = Some(5);
let y = x.map(|i| i + 1); // Some(6)
let z = x.unwrap_or(0);   // 5, или 0 если None
```

### Result<T, E>
```rust
// Result может быть Ok(T) или Err(E)
use std::fs::File;

let f = File::open("hello.txt");

match f {
    Ok(file) => println!("Файл открыт успешно"),
    Err(error) => println!("Ошибка открытия файла: {:?}", error),
}

// Использование методов Result
let f = File::open("hello.txt").unwrap(); // Паникует при ошибке
let f = File::open("hello.txt").expect("Не удалось открыть файл");
let f = File::open("hello.txt")?; // Распространение ошибки (только в функциях, возвращающих Result)
```

## Структуры (Structs)

### Именованные поля
```rust
struct User {
    username: String,
    email: String,
    sign_in_count: u64,
    active: bool,
}

// Создание экземпляра
let user1 = User {
    email: String::from("someone@example.com"),
    username: String::from("someusername123"),
    active: true,
    sign_in_count: 1,
};

// Изменение полей
let mut user1 = User {
    email: String::from("someone@example.com"),
    username: String::from("someusername123"),
    active: true,
    sign_in_count: 1,
};
user1.email = String::from("anotheremail@example.com");
```

### Кортежные структуры (Tuple Structs)
```rust
struct Color(i32, i32, i32);
struct Point(i32, i32, i32);

let black = Color(0, 0, 0);
let origin = Point(0, 0, 0);
```

### Unit-подобные структуры
```rust
struct AlwaysEqual;

let subject = AlwaysEqual;
```

## Перечисления (Enums)

### Базовое перечисление
```rust
enum IpAddrKind {
    V4,
    V6,
}

let four = IpAddrKind::V4;
let six = IpAddrKind::V6;
```

### Перечисления с данными
```rust
enum IpAddr {
    V4(u8, u8, u8, u8),
    V6(String),
}

let home = IpAddr::V4(127, 0, 0, 1);
let loopback = IpAddr::V6(String::from("::1"));

// Более сложный пример
enum Message {
    Quit,
    Move { x: i32, y: i32 },
    Write(String),
    ChangeColor(i32, i32, i32),
}
```

### Обработка перечислений с match
```rust
enum Coin {
    Penny,
    Nickel,
    Dime,
    Quarter,
}

fn value_in_cents(coin: Coin) -> u8 {
    match coin {
        Coin::Penny => 1,
        Coin::Nickel => 5,
        Coin::Dime => 10,
        Coin::Quarter => 25,
    }
}
```

### if let
```rust
let some_value = Some(3u8);
if let Some(3) = some_value {
    println!("три");
}
```

## Функции

### Функции с параметрами
```rust
fn another_function(x: i32, y: i32) {
    println!("Значение x: {}", x);
    println!("Значение y: {}", y);
}
```

### Функции с возвращаемым значением
```rust
fn five() -> i32 {
    5  // Без точки с запятой - возвращаемое значение
}

fn plus_one(x: i32) -> i32 {
    x + 1  // Возвращает x + 1
}

// Явный return
fn plus_one_explicit(x: i32) -> i32 {
    return x + 1;
}
```

### Замыкания (Closures)
```rust
let add_one = |x: i32| -> i32 { x + 1 };
let add_one_short = |x| x + 1; // Типы выводятся автоматически

let result = add_one(5); // 6

// Захват переменных из окружения
let x = 4;
let equal_to_x = |z| z == x;
let y = 4;
assert!(equal_to_x(y));
```

## Типы времени жизни (Lifetimes)

### Аннотации времени жизни
```rust
// Время жизни параметров и возвращаемого значения
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() {
        x
    } else {
        y
    }
}

// Время жизни в структурах
struct ImportantExcerpt<'a> {
    part: &'a str,
}
```

## Типы-указатели (Smart Pointers)

### Rc<T> (Reference Counted)
```rust
use std::rc::Rc;

let a = Rc::new(5);
let b = Rc::clone(&a);
let c = Rc::clone(&a);
// Теперь a, b и c владеют одним и тем же значением
```

### Arc<T> (Atomically Reference Counted)
```rust
use std::sync::Arc;
use std::thread;

let a = Arc::new(5);
let b = Arc::clone(&a);

let handle = thread::spawn(move || {
    println!("Значение: {}", b);
});
```

### RefCell<T> (Внутренняя изменяемость)
```rust
use std::cell::RefCell;

let data = RefCell::new(5);
{
    let mut borrow = data.borrow_mut();
    *borrow += 1;
}
println!("{}", data.borrow()); // 6
```

## Типы-примитивы для многопоточности

### Mutex<T>
```rust
use std::sync::Mutex;

let m = Mutex::new(5);
{
    let mut num = m.lock().unwrap();
    *num = 6;
}
println!("m = {:?}", m); // m = Mutex { data: 6 }
```

### Channel
```rust
use std::sync::mpsc;

let (tx, rx) = mpsc::channel();
tx.send(String::from("привет")).unwrap();
let received = rx.recv().unwrap();
println!("Получено: {}", received);
```

## Дополнительные типы

### Never Type (!)
```rust
fn bar() -> ! {
    // Функция, которая никогда не возвращает управление
    loop {
        // бесконечный цикл
    }
}
```

### Динамически размещаемые типы (DST - Dynamically Sized Types)
```rust
// str - динамически размещаемый тип
let s: &str = "hello";

// [T] - динамически размещаемый тип
let slice: &[i32] = &[1, 2, 3];
```

## Примеры использования типов

### Комплексный пример
```rust
use std::collections::HashMap;

struct Person {
    name: String,
    age: u32,
    hobbies: Vec<String>,
}

enum Status {
    Active,
    Inactive,
    Suspended,
}

fn main() {
    // Создание структуры
    let person = Person {
        name: String::from("Иван"),
        age: 30,
        hobbies: vec![
            String::from("чтение"),
            String::from("программирование"),
        ],
    };

    // Использование HashMap
    let mut status_map: HashMap<String, Status> = HashMap::new();
    status_map.insert(person.name.clone(), Status::Active);

    // Обработка Option
    match status_map.get(&person.name) {
        Some(Status::Active) => println!("Пользователь активен"),
        Some(Status::Inactive) => println!("Пользователь неактивен"),
        Some(Status::Suspended) => println!("Пользователь заблокирован"),
        None => println!("Статус не найден"),
    }

    // Работа с вектором
    for hobby in &person.hobbies {
        println!("Хобби: {}", hobby);
    }
}
```

## Заключение

Rust предоставляет богатую систему типов, которая обеспечивает безопасность памяти и предотвращает многие распространенные ошибки на этапе компиляции. Понимание различных типов данных и их использования является основой для эффективной работы с Rust.

